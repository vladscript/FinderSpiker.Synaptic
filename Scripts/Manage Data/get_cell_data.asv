% Make Cell Data of the Vector Signal
% in Intervals
% Input
% x: vector
% Intervasl: Matrix of intervlas:[a1:b1;...]
% Output
% X cell each element is the vector ofr each interval
function X=get_cell_data(x,Intervals)
%% Setup
Nc=size(Intervals,1);
